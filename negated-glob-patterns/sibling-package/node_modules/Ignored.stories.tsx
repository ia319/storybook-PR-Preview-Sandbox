import type { Meta, StoryObj } from '@storybook/react';

const Ignored = () => <div>Ignored Component</div>;

const meta = {
  title: 'Sibling/Ignored',
  component: Ignored,
} satisfies Meta<typeof Ignored>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Primary: Story = {};
